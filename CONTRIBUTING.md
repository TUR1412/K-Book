# 贡献指南

感谢参与 K-Book 项目。请遵循以下约定以保持仓库一致性。

## 开发环境
- Java 8+
- Maven 3.8+
- MySQL 5.7+

## 提交前检查
- UI 变更需更新资源版本号
- 运行 `mvn -DskipTests package` 确保构建成功
- 不提交 `.env` 或敏感配置

## PR 规范
- 标题清晰（功能/修复/优化）
- 描述包含动机、方案与验证方式
- 涉及数据结构变更需同步更新 `deploy/init.sql`
